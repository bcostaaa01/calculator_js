const subtraction = () => {
  for (let num = 1; num < 11; num++) {
    const subtractor = 3;
    const formula = `${num} - ${subtractor}`;
    const subtractionResult = `${num}` - `${subtractor}`;
    console.log(formula + " = " + subtractionResult);
  }
};

const division = () => {
  for (let num = 1; num < 11; num++) {
    const divisor = 3;
    const formula = `${num} / ${divisor}`;
    const divisionResult = `${num}` / `${divisor}`;
    console.log(formula + " = " + divisionResult);
  }
};

const multiplication = () => {
  for (let num = 1; num < 11; num++) {
    const multiplier = 3;
    const formula = `${num} * ${multiplier}`;
    const multiplicationResult = `${num}` * `${multiplier}`;
    console.log(formula + "\n = " + multiplicationResult);
  }
};

const addition = () => {
    for (let num = 1; num < 11; num++) {
        const addend = 3;
        const formula = `${num} + ${addend}`;
        const additionResult = `${num}` + `${addend}`;
        console.log(formula + ' = ' + additionResult);
    }
}
